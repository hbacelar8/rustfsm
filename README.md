# RustFSM

A full static Rust finite state machine library.

Compatible with `no_std` and embedded environments.

## Example

Run the `mario` example with a well known state machine.

```bash
cargo run --example mario
```

> It's a me! 🍄
